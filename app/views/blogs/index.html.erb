<% content_for :menu do %>
   <li><%= link_to "Add Post", new_blog_path %></a></li>
<% end %>

<div class="faded-box">
  <ul class="blog-post-list">
    <% @blogs.each do |blog| %>
      <li><%= link_to "#{human_date(blog.created_at)} @ #{human_time(blog.created_at)}" , blog_path(blog) %></li>
    <% end %>
  </ul>
</div>
